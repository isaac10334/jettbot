# Jettbot AI Notes

This repo follows the project-level IRA guidance in `docs/ai/ARCHITECTURE.md`.

AI-facing work should keep tool adapters thin, put committed domain behavior in services, and share prompt/context assembly across text and voice as that layer matures. The canonical current handoff is `docs/ai/HANDOFF.md`.
