# Jettbot Handoff

This is the only canonical handoff file. Use it as external project memory: keep current truths, risks, and linked next prompts here; do not turn it into a full command log.

## Current Work Landscape

- Jettbot should become a Discord bot with one shared brain across text and voice: personality, memory, realtime self-awareness, voice-channel awareness, durable event logging, and eventually adaptive behavior.
- YouTube playback works through the WAV path: plain search text resolves through `ytsearch1:`, direct URLs resolve normally, `yt-dlp` downloads `bestaudio`, FFmpeg prepares `audio.wav` as `pcm_s16le` 48 kHz stereo, and the Rust sidecar plays it through Songbird `File` input with Symphonia `pcm`/`wav` features enabled.
- Voice now has one proven live end-to-end success. The 2026-05-05 8:16 PM session proved join, receive enablement, SSRC attribution through `SpeakingStateUpdate`, decoded known-user audio, AssemblyAI `Turn` messages, known-user transcripts, LLM response generation, ElevenLabs PCM streaming, and sidecar playback through Discord. That same run exposed the next bug: four TTS streams overlapped because playback enqueue waited for sidecar input-send completion, not actual Songbird playback end. The current code adds a first `ConversationEngineRuntime` and per-guild speech serialization so Jettbot speech queues until reliable sidecar playback end.
- Text context and memory should move toward a database-backed observation log. Discord fetch is for bounded context windows, startup/reconnect backfill, gap repair, and explicit read commands, not the default source of truth on every response.
- Prompt/personality/context assembly is currently too scattered. Future voice and text responses should share one explicit context assembly path.

## Architecture Thesis

- IRA boundaries matter: services own committed domain truth and database writes; runtimes own lifecycle, time, root Run, installed modules, signals, session tasks, and cleanup; bridges adapt Discord, OpenAI, AssemblyAI, yt-dlp, ffmpeg, and the Rust audio sidecar; policies react to signals/runtime state and issue typed service commands.
- Signals are occurrences. Stores are current state. Tasks and runs own async/cancelable work. UI or Discord handlers should render/dispatch only, not own business logic. Revisit `@loop-kit/common` primitives when expanding realtime systems: `Runtime`, `Installer`, `Run`, `Task`, `Signal`, `Store`, `Console`, `Result`, `timeout`, `retry`, `concurrently`, `race`, and standard `AsyncDisposableStack`.
- The conversation engine decides whether/how to speak; response generation writes text; the speech controller serializes playback. The engine must not call ElevenLabs, Discord, or the Rust sidecar directly.
- Memory should be layered: raw observation log, episodes, semantic memories, procedural memories, and realtime self-state/body schema.
- Retrieval should start pragmatic: Turso/SQLite indexes plus TypeScript filtering/reranking. Add vector retrieval after the durable event/memory shape is real. Do not start with Vespa or Meilisearch.
- Audio and text logs can contain private Discord data. Any retention, hindsight processing, or user profile work needs explicit deletion, privacy, and storage-cost decisions.

## Primary Prompt Roadmap

Run these prompts in order:

1. [Voice conversation engine and latency](prompts/voice-conversation-engine-latency.md) - improve turn-taking, stop self-overlap, measure latency, and prepare for multi-user voice.
2. [Memory/database foundation](prompts/memory-database-foundation.md) - build durable observation and memory foundations using Turso/libSQL and a Drizzle-first migration path.
3. [Prompt/personality/context assembly](prompts/prompt-personality-context-assembly.md) - centralize personality and assemble shared text/voice prompt context.
4. [AwarenessRuntime/self-state design](prompts/awareness-runtime-self-state.md) - design realtime self-awareness for prompt context and policies.
5. [YouTube loading latency and streaming](prompts/youtube-loading-latency-streaming.md) - reduce time-to-audible playback without regressing the reliable WAV/Songbird path.
6. [Observability dashboard](prompts/observability-dashboard.md) - future IRA bridge for logs, metrics, measurements, voice sessions, YouTube phases, and simple charts.

## Secondary Prompt Drafts

- [Voice pipeline debug](prompts/live-voice-test.md) - next live voice prompt; stage-gated around known-user transcripts before LLM/TTS claims.
- [Audio debugging reference](audio-debugging-reference.md) - hard-won audio/voice failure modes and non-regression notes.

## Archived Prompt References

- [YouTube playback components queue UI](prompts/archived/youtube-playback-components-queue-ui.md) - completed.
- [Audio sidecar common failure](prompts/archived/audio-sidecar-common-failure.md) - completed locally; live Discord verification remains separate.
- [Voice pipeline observability/testing](prompts/archived/voice-pipeline-observability-testing.md) - initial local implementation completed.
- [Shutdown voice cleanup](prompts/archived/shutdown-voice-cleanup.md) - completed locally.
- [Image search polish](prompts/archived/image-search-polish.md) - completed locally.
- [Memory context layer](prompts/archived/memory-context-layer.md) - retired into memory/database foundation.

## Risks And Non-Goals

- Do not implement fake features or broad platform scaffolding that is not wired to a working vertical slice.
- Do not flatten this into generic RAG bot advice. Jettbot needs a Discord-native shared brain across text, voice, runtime awareness, and policies.
- Do not introduce large dependencies unless a prompt explicitly asks to evaluate them.
- Do not build a large observability dashboard before structured logs, metrics, traces, and audio artifacts exist.
- Do not treat Discord presence or casual user claims as truth. Presence is a signal with confidence/provenance, and Jettbot should ask clarifying questions when needed.
- `bun dev` connects to Discord. Do not run it as routine verification without the user's explicit intent for a live bot session.

## Recent Verification

- 2026-05-05 8:16 PM realtime voice success and conversation-engine v1: latest session `logs/realtime/5-5-26_8-16-PM_624393515249434644_1150310214139138170_e5247532-b39f-4323-baf4-84b6fe986550` had no missing stages: AssemblyAI events `{"open":1,"Begin":1,"SpeechStarted":2,"Turn":4,"close":1}`, known user `377268939035639810`, mapped SSRC `20098`, and 4 playback starts/finishes. The PCM fallback worked: env requested `opus_48000_128`, logs showed effective `pcm_24000`, ElevenLabs returned `audio/pcm`, and sidecar playback reached `playable/end`. Latency observed: final transcript to LLM start `82-139ms`, LLM duration `1316-1425ms`, TTS request to provider response `642-708ms`, TTS request to playable `697-750ms`. The run also exposed `Playback overlap: overlappingStreams=4`. Implemented v1 `ConversationEngineRuntime`, deterministic direct-address/stop/wait/ignore decisions, per-guild speech queueing, playback-end waiting for TTS, interruption handling for stop/cancel style commands, latency correlation IDs, and analyzer overlap reporting. Focused verification passed: conversation engine, voice pipeline, voice service, and diagnostics tests (`23` tests), plus `bun run typecheck`.
- 2026-05-05 2:16 PM realtime voice playback fix: latest session `logs/realtime/5-5-26_2-16-PM_624393515249434644_1150310214139138170_66c1c5e6-d098-4e17-a2bb-f9bf185caaf4` showed the attribution race fix worked. Analyzer output: AssemblyAI events `{"open":1,"Begin":1,"SpeechStarted":2,"Turn":4}`, 4 transcripts with 2 finals, known user `377268939035639810`, `speakingStateMapped=1`, mapped SSRC `18101`, decoded voice ticks and first decoded chunk, LLM response artifacts, and TTS request artifacts. Playback failed at `tts.playback.enqueue_failed` with `invalid audio format: opus_48000_128` after ElevenLabs returned `contentType: audio/opus`. Fix: unsupported ElevenLabs stream formats are resolved to `pcm_24000` before the HTTP request and before sidecar playback, while `text/tts.jsonl` records `requestedFormat`, effective `format`, and `formatOverridden`. Repeated Songbird UDP decode warnings are still written to `logs/sidecar.log` but no longer promoted to console. Analyzer now treats `decoded_voice_tick`/`first_decoded_chunk` as Discord receive evidence. Focused verification passed: `bun test apps/bot/src/__tests__/VoicePipelineOffline.test.ts apps/bot/src/__tests__/VoiceSessionDiagnostics.test.ts apps/bot/src/__tests__/RustSidecarService.test.ts` (`9` tests).
- 2026-05-05 voice attribution race fix: latest session `logs/realtime/5-5-26_5-01-AM_624393515249434644_1150310214139138170_2d1873a2-1648-4c30-9af5-8b280ebec650` showed AssemblyAI still working (`open`, `Begin`, `SpeechStarted`, 14 `Turn`, 4 finals) but all transcripts were diagnostic `unknown_ssrc:*`; analyzer now reports `attribution_mapping_missing` separately from known-user transcripts. Root cause was the sidecar `Receiver::act()` returning early while receive was disabled, which could drop `ClientConnect`/`SpeakingStateUpdate` mapping events that arrive during join before TypeScript sends `StartReceive`. The sidecar now gates only `VoiceTick` audio on receive state; mapping and disconnect events are processed regardless. Realtime debug now buffers early `VoiceDebug` events until `JoinedVoice` starts the session folder and writes a `sidecar_runtime` fingerprint. Analyzer now counts `client_connect_mapped`, `speaking_state_mapped`, early buffered events, and mapped SSRCs. Verification passed: focused realtime debug/diagnostics/transcription/offline pipeline Bun tests (`13` tests), `bun run typecheck`, `cargo fmt --check`, sidecar tests (`23` Rust tests), `bun run sidecar:build`, and full `bun run check` (`75` Bun tests and `23` Rust tests). Realtime logs were cleaned to keep only the latest 3 sessions.
- 2026-05-05 realtime voice attribution fix: latest session `logs/realtime/5-5-26_4-21-AM_624393515249434644_1150310214139138170_487863ae-ca0c-4dee-b901-a21ad4c3a481` showed AssemblyAI working (`open`, `Begin`, `SpeechStarted`, 24 `Turn`, 9 finals) but all transcripts were diagnostic `unknown_ssrc:*`; missing stages were known-user transcripts, LLM response, TTS stream, and Discord playback. Patched local `songbird 0.6.0` via `[patch.crates-io]` to expose Discord `ClientConnect` and populate SSRC maps from `audio_ssrc/user_id`, added sidecar `client_connect_mapped` diagnostics and tests, carried odd PCM bytes across streamed playback chunks, added speaker profile metadata plus `text/stitched-transcripts.jsonl`, expanded LLM/TTS logs, saved ElevenLabs raw output plus sidecar input/f32 debug artifacts, added `bun run voice:diagnose-audio`, ran `voice:cleanup-logs` keeping only the latest 3 realtime sessions, and generated a captured-audio diagnostic WAV under `logs/audio-diagnostics`. Verification passed: focused Bun voice diagnostics tests (`10` tests), `bun run typecheck`, sidecar tests (`19` Rust tests), `cargo fmt --check`, `bun run sidecar:build`, and full `bun run check` (`73` Bun tests and `19` Rust tests).
- 2026-05-05 voice pipeline diagnostics/testing pass: latest realtime sessions were enumerated and older realtime folders were cleaned so only the latest 3 remain. Latest session `logs/realtime/5-5-26_3-45-AM_624393515249434644_1150310214139138170_5f816f00-7100-45c3-863f-ac747d9efffb` shows AssemblyAI working (`open`, `Begin`, `SpeechStarted`, and 347 `Turn` messages), 346 transcript entries, all diagnostic `unknown_ssrc:*` speakers, no known-user transcript turns, and no live LLM/TTS artifacts. Same session showed YouTube/WAV playback reaching `playable`, a two-second probe, and normal `end`. Added `bun run voice:analyze`, `bun run voice:cleanup-logs`, RIFF chunk-aware TypeScript WAV validation, offline known-user transcript -> LLM -> TTS -> sidecar playback regression coverage, sidecar script fail-fast propagation for Cargo failures, and additional Rust sidecar attribution diagnostics (`speaking_state_ssrc_mapped`, low-frequency `decoded_voice_tick`). Verification passed: focused voice/audio/diagnostics tests (`23` tests), `bun run typecheck`, `cargo fmt --check`, sidecar tests (`15` Rust tests), `bun run sidecar:build`, and full `bun run check` (`71` Bun tests and `15` Rust tests). A stale `bun dev`/sidecar process from 3:45 AM was stopped because it locked the release sidecar binary during the first build attempt.
- 2026-05-05 image search diagnostics/reliability pass: `.img` and `/img` both route through the same `runSearch()` and `renderSession()` path, now covered by a regression test. Brave response diagnostics are logged through the normal bot console/file log as `image.search.brave.response`, including request params, query alteration, `show_strict_warning`, `might_be_offensive`, raw/mapped/dropped counts, and first-result URL/dimension/thumbnail/placeholder/confidence fields. Render choices are logged as `image.render.selection`. Unknown original dimensions now prefer Brave thumbnails when available instead of optimistic original hotlinks, because unknown source URLs are a likely Discord embed non-load path. Verification passed: focused image tests, `bun run typecheck`, and full `bun test` (`66` tests).
- 2026-05-05 image search safe-search/display fix: Brave Image Search must keep `safesearch=off` explicitly because the API defaults to `strict` when omitted. Added a code reminder beside `defaultImageSafeSearch`, added a regression test that verifies the Brave request sends `safesearch=off`, and stopped using `properties.placeholder` as a Discord display thumbnail because Brave documents it as a small placeholder URL, which can render as grey/placeholder-like instead of the real image. Verification passed: focused `bun test apps/bot/src/__tests__/BraveImageSearchService.test.ts apps/bot/src/__tests__/DiscordImagePolicy.test.ts apps/bot/src/__tests__/ImageSearchTool.test.ts` and `bun run typecheck`.
- 2026-05-05 live voice investigation: latest realtime session `logs/realtime/5-4-26_9-34-PM_624393515249434644_1150310214139138170_e3391a0e-86fb-4011-a065-e5a4a43aa1ef` showed `receive_enabled`, `UserSpeakingStart`, `first_decoded_chunk`, 2,200+ `assemblyai.audio_sent` entries, and valid WAV artifacts (`discord-input.wav`: 48 kHz stereo PCM, 65.2s; `assemblyai-input.wav`: 16 kHz mono PCM, 65.2s, mean volume about -26.4 dB). `transcription-sessions.jsonl` only showed session starting/started, with no `transcripts.jsonl`, LLM, or TTS files. Added AssemblyAI session-event diagnostics for open/message/parse-error/error/close into `text/transcription-sessions.jsonl`. Verification passed: `bun run typecheck`, focused `bun test apps/bot/src/__tests__/TranscriptionPipeline.test.ts apps/bot/src/__tests__/AudioNormalizationService.test.ts`. Needs a fresh live voice run; expected next evidence is `assemblyai.session.event` entries showing `Begin`, `Turn`, `Termination`, close, or parse/error details.
- 2026-05-05 follow-up live voice test: session `logs/realtime/5-4-26_9-56-PM_624393515249434644_1150310214139138170_dbc93e03-8cb0-4a2d-9759-72c8205ac08f` was a real spoken-phrase test, but it only reached `join_succeeded`, `receive_enabled`, `first_voice_tick`, and one `unknown_ssrc`; it produced no `UserSpeakingStart`, `UserAudioChunk`, WAVs, transcripts, LLM, or TTS. Added a Rust sidecar buffer for decoded `VoiceTick` audio that arrives before the SSRC-to-user `SpeakingStateUpdate`; buffered chunks flush when the mapping appears and emit `unknown_ssrc_resolved`. Also added a diagnostic fallback after 10 unmapped decoded chunks: emit real audio with user id `unknown_ssrc:<ssrc>` and `VoiceDebug stage=unknown_ssrc_audio_fallback`, so AssemblyAI can be tested separately from Discord speaker attribution. Verification passed: `cargo fmt --check`, `scripts/sidecar.ps1 test` (`15` Rust tests), and `bun run sidecar:build`. Needs another short live voice run; expected evidence is either real-user `UserAudioChunk`, `unknown_ssrc_audio_fallback` plus AssemblyAI events, or no decoded chunks.
- 2026-05-05 AssemblyAI packet sizing fix: correct live run `logs/realtime/5-4-26_10-17-PM_624393515249434644_1150310214139138170_85009c5c-38e5-429b-a208-d1cba7908041` reached the unknown-SSRC fallback, wrote valid WAVs, opened AssemblyAI sessions, then closed with code `3007`. Root cause was sending 640-byte/20ms normalized chunks; AssemblyAI v3 requires 50-1000ms input packets and may reject bursts faster than realtime. The transcription pipeline now coalesces normalized 16 kHz mono PCM into 3200-byte/100ms packets before provider send, drops cleanup tails shorter than 50ms with `assemblyai.audio_dropped_short_tail`, logs coalesced `assemblyai.audio_sent` sequence ranges/duration, resets closed/error sessions for later speech, surfaces AssemblyAI error payload fields, and keeps `unknown_ssrc:*` transcripts diagnostic-only by not forwarding them into conversation/memory. Verification passed: focused `bun test apps/bot/src/__tests__/TranscriptionPipeline.test.ts`, `bun run typecheck`, and full `bun run check` (`61` Bun tests and `15` Rust tests). Needs fresh live voice run; expected evidence is `assemblyai.audio_sent` with `byteLength=3200`/`durationMs=100`, followed by `Turn` messages instead of close code `3007`.
- 2026-05-04 shutdown/image/prompt cleanup pass: archived completed prompts, added active [YouTube loading latency and streaming](prompts/youtube-loading-latency-streaming.md) and [Observability dashboard](prompts/observability-dashboard.md) prompts, added runtime-owned voice shutdown cleanup before sidecar disposal, added sidecar shutdown leave-all backstop, and improved image embeds to prefer reasonable original URLs over 500px Brave thumbnails with render quality metrics and quiet expired paging. Verification passed: `bun run typecheck`, focused voice/image/YouTube tests, full `bun test` (`58` tests), `cargo fmt --check`, sidecar tests (`15` Rust tests), and full `bun run check`.
- 2026-05-04 multi-guild memory schema startup fix: `TursoMemoryService.initialize()` now creates base tables, repairs legacy nullable scope columns, then creates guild-scoped indexes. This fixes startup against pre-multi-guild databases where `messages.guild_id` did not exist. Added a regression test using an old file-backed libSQL schema for `messages`, `transcript_turns`, and `tool_events`. Verification passed: focused `bun test apps/bot/src/__tests__/MemoryService.test.ts`, `bun run typecheck`, and full `bun run check` (`53` Bun tests and `15` Rust tests).
- 2026-05-04 audio sidecar/voice observability pass: added sidecar `VoiceDebug` events for join/receive boundaries, made `StartReceive`/`StopReceive` explicit receive-state commands, added stream/file playback lifecycle `PlaybackDebug` stages, stopped treating stream input close as Songbird playback completion, and added f32 file alignment validation before `PlayAudioFile`. TypeScript now routes `VoiceDebug` to `text/voice-events.jsonl`, records join command failures on `VoiceService.state`, and includes local WAV/raw PCM validation helpers. Focused tests passed, full `bun test` passed (`39` tests), `bun run typecheck` passed, `scripts/sidecar.ps1 test` passed (`12` Rust tests), `bun run check` passed, and `bun run sidecar:build` passed cleanly. Live `bun dev`/Discord verification was not run because this shell is missing `DISCORD_BOT_TOKEN`, `DISCORD_APPLICATION_ID`, `ASSEMBLYAI_API_KEY`, `ELEVENLABS_API_KEY`, `ELEVENLABS_VOICE_ID`, and `AI_GATEWAY_API_KEY`.
- 2026-05-04 YouTube raw playback fix: latest live logs showed `PlaybackDebug stage=end/error` with `DecodeError("no compatible track found")` immediately after `PlayAudioFile`, proving Songbird rejected the seekable `RawAdapter<File>` f32 PCM input. Replaced file playback with a non-seekable `F32FilePlaybackReader` behind `RawAdapter`, added a unit test proving the reader is non-seekable, made TypeScript `playAudioFile` wait for Songbird `playable` or fail fast on `error/end`, and promoted playback playable/error/end debug to the main console. `bun run sidecar:build` passed, `bun run check` passed, and Rust sidecar tests now pass with `13` tests. Needs fresh live `/youtube` verification; expected success signal is `sidecar.playback.debug` with `stage: "playable"` instead of `DecodeError("no compatible track found")`.
- 2026-05-04 YouTube WAV playback rewrite: fresh live logs still showed `DecodeError("no compatible track found")` for `RawAdapter`, so the YouTube file path now prepares `audio.wav` as `pcm_s16le`, `48000Hz`, stereo and sends `wav_pcm_s16le_48000_stereo` to `PlayAudioFile`. The sidecar now validates canonical PCM WAV headers and uses Songbird `File` input for WAV files. Added direct `symphonia = "=0.5.2"` with `pcm` and `wav` features so Songbird's disabled-default Symphonia dependency has the required decoder/format support. Added tests for WAV format parsing, WAV validation, and the ffmpeg WAV prepare command. `bun run sidecar:build` passed and `bun run check` passed with `41` Bun tests and `15` Rust tests. Needs fresh live `/youtube`; expected log should mention `audio.wav`, `format=wav_pcm_s16le_48000_stereo`, `input_created` as `wav file input created`, then `stage: "playable"`.
- 2026-05-04 YouTube WAV validator fix: live run with `weezer sweater song` created a real `audio.wav` at `C:\Users\ijhar\AppData\Local\Temp\jettbot-youtube-KL7yzs\audio.wav` (`48,828,272` bytes). `ffprobe` verified `codec_name=pcm_s16le`, `sample_rate=48000`, `channels=2`, `duration=254.313500`. Sidecar rejected it because the validator assumed `data` was at byte 36; FFmpeg inserted a valid `LIST/INFO` chunk before `data`. Replaced fixed-offset WAV validation with RIFF chunk parsing and added YouTube debug JSONL entries for yt-dlp command/exit, downloaded file stats, ffmpeg command/exit, and prepared WAV stats. `bun run check`, `bun run sidecar:build`, and `cargo fmt --check` passed. Needs fresh live `/youtube`; if it fails, check `text/youtube.jsonl` for `youtube.file.ytdlp.*`, `youtube.file.ffmpeg.*`, `youtube.file.ready` and `text/voice-events.jsonl` for `PlaybackDebug`.
- 2026-05-04 working YouTube playback lesson: the final fix was not “better streaming”; it was using a boring, supported file format and enabling the actual decoder stack. The failed paths were Windows Bun stdin piping, high-frequency IPC PCM chunk streaming, `RawAdapter<File>` f32 PCM, and a too-strict WAV validator. The working path is query/URL resolve with `yt-dlp`, download `bestaudio`, FFmpeg to `audio.wav` (`pcm_s16le`, 48 kHz, stereo), sidecar `PlayAudioFile` with `wav_pcm_s16le_48000_stereo`, RIFF chunk-aware WAV validation, Songbird `File` input, and Symphonia `pcm`/`wav` features. Never regress this back to raw f32 file playback or fixed-offset WAV parsing.
- 2026-05-04 YouTube queue/UI follow-up: completed the embed/button queue UI polish for [YouTube playback components queue UI](prompts/youtube-playback-components-queue-ui.md). `YoutubePlaybackService` owns current item, queue, skip, cancel-all, and sidecar track-end observation; it now keeps status `preparing` until `VoiceService.playAudioFile` confirms Songbird `playable`. `/youtube` enqueues resolved search/URL requests and sends or edits one public playback message with status, duration, queue length, preview, Skip, and Stop All buttons. `/stop` uses the same cancel-all path, and the AI `youtube_audio` tool enqueues instead of directly playing. Verification: `bun run typecheck`, focused YouTube/button tests, `bun test` (`50` tests), Rust sidecar tests (`15` tests), `bun run sidecar:build`, and `bun run check` passed. Live Discord verification was not run.
- 2026-05-04 YouTube fast-track log follow-up: recent logs showed resolve success followed by immediate `youtube.stream.exit` with `ytdlpCode=143` and `ffmpegCode=143`, no `PlaybackChunk`, and immediate `PlaybackFinished`. Root cause was the first streaming pipe path: local `ffmpeg` is `C:\Python313\Scripts\ffmpeg.exe` from 2013 and rejects `-hide_banner`; after removing that, Bun stdin piping still hung on Windows. Hybrid temp-file playback produced a real first PCM chunk in a service smoke test (`2592` bytes), and `bun run check` passed. Live Discord playback still needs a fresh run.
- 2026-05-04 YouTube pacing follow-up: live logs for `weezer sweater song` showed FFmpeg decoded successfully but the bot sent `10835` `PlayAudioStreamChunk` commands and emitted `PlaybackFinished` in about `1.5s` for a `254s` track. Added realtime pacing/coalescing for YouTube PCM playback with about `500ms` lead. `bun run check` passed. Needs another live `/youtube` attempt to verify audible duration.
- 2026-05-04 YouTube sidecar file playback follow-up: latest live retry still did not produce audible playback. Moved YouTube off high-frequency TypeScript IPC streaming by adding sidecar `PlayAudioFile`; TypeScript now prepares `pcm_f32le_48000_stereo` via FFmpeg and sidecar/Songbird reads the file directly. Rebuilt release sidecar with `bun run sidecar:build`; `bun run check` passed. Needs fresh live `/youtube` verification.
- 2026-05-04 YouTube sidecar observability follow-up: `file.ready` occurred and realtime logs did show `PlaybackStarted`, but no audible audio. Verified prepared `audio.f32le` was valid (`254.31s`, `97.6MB`). Added sidecar `PlaybackDebug` events for `PlayAudioFile`: command received, input created, track submitted, Songbird playable/error/end, and a two-second probe; switched file playback to `play_only_input`. Rebuilt release sidecar; `bun run check` passed. Fresh live run should inspect `text/voice-events.jsonl` for `PlaybackDebug` stages.
- 2026-05-04 YouTube fast-track implementation: `bun run typecheck`, focused YouTube tests, full `bun test`, and full `bun run check` passed. Live Discord playback was not run.
- 2026-05-04 roadmap pass: docs-only update completed for `docs/ai/HANDOFF.md` and `docs/ai/prompts/*`; required prompt files and concept references were checked with `rg`.
- Previous known verification: TypeScript typecheck passed, full Bun tests passed, Rust sidecar tests passed, and `bun run check` passed after the audio-first debug implementation.
- `ffprobe` accepted a generated AssemblyAI debug WAV as `pcm_s16le`, `sample_rate=16000`, `channels=1`, `duration=0.100000`.
- Live Discord voice verification is still pending. Do not claim the realtime path is proven until a fresh Discord voice test confirms it.

## Handoff Maintenance Rules

- Keep this file concise and current. Replace stale notes instead of appending a new chronology.
- Put detailed future-work context into a small linked prompt under `docs/ai/prompts/`.
- Every auxiliary agent-memory or prompt file must be linked from this file.
- Summarize meaningful verification. Do not list every touched file or every command; Git and local shell history are better suited for that bookkeeping.
